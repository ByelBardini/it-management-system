{
  "name": "it-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo InfraHub — backend (server/) + frontend (client/) rodando juntos no dev.",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run start\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run start",
    "dev:client": "npm --prefix client run dev",
    "postinstall": "npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
